// should control whether or not a letter appears as a "_" or as itself on-screen
var letter = [];
var placeHolder = '_';

if (letter == true) {
	console.log(letter);
}else {
	placeHolder = placeHolder;
}

module.exports = methodsWordJs;